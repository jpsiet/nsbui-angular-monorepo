import { createAction } from '@ngrx/store';

export const spanish = createAction('[Counter Component] Increment');
export const french = createAction('[Counter Component] Decrement');
export const reset = createAction('[Counter Component] Reset');
